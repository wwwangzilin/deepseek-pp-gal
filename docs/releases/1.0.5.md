## DeepSeek++ 1.0.5

1.0.5 是项目上下文和 Agent 续跑稳定性更新，重点让项目里的新对话启动更直接、项目对话标题更准确，同时减少 inline agent 重复续跑、内部续跑消息外露、识图上传误判和代码块下载按钮遮挡内容的问题。

### 主要变化

- **项目对话组织**：项目侧边栏新增「在项目中开启新对话」入口，新会话会直接继承所选项目上下文。
- **项目标题刷新**：项目对话会优先使用真实历史标题，默认 DeepSeek 标题或未命名占位不会覆盖已经保存的有效标题。
- **Agent 续跑稳定性**：inline agent 在已经给出完整答案时减少重复续跑，并隐藏内部续跑消息，让最终回答和历史记录更干净。
- **识图与代码块体验**：识图图片上传对 DeepSeek 返回的成功但待定审核状态更兼容；代码块下载按钮改为浮动定位，减少对代码内容布局的影响。
- **开源许可证**：项目许可证和包元数据同步为 Apache-2.0，README 徽章与源码包信息保持一致。
- **回归覆盖**：新增项目对话标题、项目侧边栏、inline agent 续跑、历史清理、识图上传和代码块下载回归测试。

### 验证

- `npm run smoke:pow`
- `npm run compile`
- `npm pack --dry-run --workspace packages/shell-host`
- `npm run build:all`
- `npm run zip:all`
- `npm run smoke:mcp`
- `npm run smoke:shell`
- `npm run verify:mcp:mock`
- `npm run verify:automation`
- `node scripts/web-search-smoke.mjs`
- `npm run prompt:freeze`
- `npm audit --audit-level=high --omit=dev`
- `npm run ci:quality`

### Chrome Web Store 更新说明

中文：

- 项目侧边栏新增「在项目中开启新对话」入口，项目对话标题会从历史记录刷新，不再被默认标题覆盖。
- Inline agent 续跑更稳：已给出完整答案时减少重复续跑，并隐藏内部续跑消息。
- 识图图片上传对 DeepSeek 返回的成功但待定审核状态兼容更好；代码块下载按钮不再改动代码块内容布局。
- 项目上下文、inline agent、识图上传、代码块下载和发布资产增加回归覆盖；项目许可证同步为 Apache-2.0。

English:

- The project sidebar adds a one-step action to start a new conversation inside a project, and project conversation titles refresh from history without being overwritten by default titles.
- Inline agent continuation is more reliable: it avoids replaying steps after a complete answer and hides internal continuation messages.
- Vision image uploads handle successful but pending/unknown review states more accurately, and code-block download buttons no longer change code-block content layout.
- Regression coverage now includes project context, inline agent continuation, Vision uploads, code-block downloads, and release assets. The project license is aligned on Apache-2.0.
